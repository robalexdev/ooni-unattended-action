name: ooni run
on: [push]

jobs:

  unattended:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Build
        run: docker build . -t ooni
      - name: Run
        run: docker run ooni

